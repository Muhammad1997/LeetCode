            // if(((startIdx - 1) >= 0) && (s[startIdx] == s[startIdx - 1]))
            // {
            //     tempStartIdx = startIdx-1;
            //     tempEndIdx = startIdx+1;
            // }
            // else
            // {
            //     endIdx = startIdx + 1;
            //     tempStartIdx = startIdx;
            //     tempEndIdx = endIdx;
            // }